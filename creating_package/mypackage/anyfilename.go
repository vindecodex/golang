package mypackage

import "fmt"

//I name this file as anyfilename because you can name this anything as long as the package name will be same with the folder where the packe belongs

func Multiply(n1 int, n2 int) {
	fmt.Println(n1 * n2)
}
